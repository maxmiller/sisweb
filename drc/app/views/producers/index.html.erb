<h1>Fornecedores</h1>

<table class="table table-striped table-hover">

  <tr>
    <th>Nome</th>
    <th>Endereço</th>
    <th>CNPJ</th>
    <th>CPF</th>
    <th>Telefone</th>
    <th>Celular</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @producers.each do |producer| %>
  <tr>
    <td><%= producer.name %></td>
    <td><%= producer.address %></td>
    <td><%= producer.cnpj %></td>
    <td><%= producer.cpf %></td>
    <td><%= producer.phone_number %></td>
    <td><%= producer.mobile_number %></td>
    <td><%= producer.email %></td>
    <td><%= link_to '', edit_producer_path(producer) , :class=>"icon-edit"%></td>
    <td><%= link_to '' ,producer,:class=>"icon-trash", method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Producer', new_producer_path %>
