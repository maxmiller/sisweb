<p id="notice"><%= notice %></p>
<fieldset>
  <legend>Grupo</legend>

<table class="table table-striped table-hover">


  <tr>
    <th>Nome</th>
    <th></th>

    <th></th>
  </tr>

<% @groups.each do |group| %>
  <tr>
    <td><%= group.name %></td>
    <td><%= link_to '',edit_group_path(group),  :class=>"icon-edit" %></td>
    <td><%= link_to '',group , :class=>"icon-trash", method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>
</fieldset>


<br />

<%= link_to 'Novo Grupo', new_group_path ,:class => 'btn btn-primary' %>
